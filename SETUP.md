# 설치 안내

전부 웹사이트에서 클릭으로 하는 일입니다. 총 20분쯤 걸립니다.
순서대로 따라 하시면 됩니다.

> 💡 이 안내는 컴퓨터를 잘 모르셔도 따라 할 수 있게 썼습니다.
> 막히는 곳이 있으면 그 단계 번호를 알려 주세요.

---

## A. 텔레그램 봇 만들기 (5분)

**왜 새로 만드나요?** 이미 쓰고 계신 경제뉴스 봇이 있지만, 텔레그램은
봇 하나에 연결할 수 있는 프로그램을 **1개로 제한**합니다. 같은 봇을 쓰면
두 봇이 서로의 버튼 클릭을 가로채서 조용히 사라집니다.

1. 텔레그램에서 `@BotFather` 를 검색해 대화를 엽니다.
2. `/newbot` 이라고 보냅니다.
3. 봇 이름을 물어보면: `로스쿨 비서`
4. 아이디를 물어보면 반드시 **`_bot` 으로 끝나야** 합니다.
   예: `mylaw_secretary_bot` — 이미 있다는 답이 오면 다른 이름으로 바꿔 보세요.
5. `123456789:AAF...` 처럼 생긴 긴 글자가 나옵니다. 이게 **봇 토큰**입니다.
   메모장에 복사해 두세요. *(비밀번호와 같습니다. 채팅창에 붙여넣지 마세요.)*

6. ⚠️ **가장 많이 빠뜨리는 단계입니다.**
   BotFather 가 준 `t.me/...` 링크를 눌러 새 봇과의 대화방을 열고,
   **`/start` 를 한 번 누르세요.**
   이걸 안 하면 봇이 먼저 말을 걸 수 없어서 알림이 하나도 오지 않습니다.

---

## B. GitHub 저장소 만들기 (3분)

1. [github.com](https://github.com) 에 로그인 → 오른쪽 위 `+` → **New repository**
2. Repository name 에 `law-secretary`
3. ⚠️ **Public 을 선택하세요.** (daily-brief 때와 반대입니다)

   **왜 공개인가요?** 아이폰 캘린더 구독은 누구나 접근할 수 있는 주소를
   요구하는데, GitHub 의 무료 웹호스팅은 공개 저장소만 지원합니다.
   여기 올라가는 것은 **학교 공지 제목·날짜·링크뿐**이고, 전부 이미
   인터넷에 공개된 정보입니다. 봇 토큰은 저장소가 아니라 다음 단계의
   **Secrets** 에 넣는데, Secrets 는 공개 저장소에서도 아무도 볼 수 없습니다.

4. **Create repository** 를 누릅니다.

---

## C. 비밀값 3개 등록 (3분)

저장소 화면에서 **Settings → Secrets and variables → Actions →
New repository secret** 을 누르고, 아래 3개를 하나씩 등록합니다.

| Name (그대로 입력) | Secret (넣을 값) |
|---|---|
| `TELEGRAM_BOT_TOKEN` | **A-5 에서 받은 새 봇 토큰** ← 경제뉴스 봇 토큰이 아닙니다! |
| `TELEGRAM_CHAT_ID` | daily brief 폴더의 `config.json` 안에 있는 `chat_id` 값 |
| `CLAUDE_CODE_OAUTH_TOKEN` | 아래 **C-2** 에서 새로 발급받습니다 |

### C-1. `chat_id` 찾기

daily brief 폴더에서 PowerShell 로:

```powershell
Get-Content config.json
```

`"chat_id": "123456789"` 처럼 나오는 숫자 부분입니다.
이 값은 봇이 달라져도 그대로 씁니다. 1:1 대화방 번호는 곧 내 계정 번호라서,
봇이 바뀌어도 같은 번호이기 때문입니다.

### C-2. `CLAUDE_CODE_OAUTH_TOKEN` 새로 발급받기

**daily-brief 저장소에서 복사해 올 수 없습니다.** GitHub Secrets 는 한 번
넣으면 다시 보여주지 않습니다. 화면에도 `***` 로만 표시되고, 꺼내는 방법이
아예 없습니다. (비밀값을 지키기 위한 설계입니다)

그러니 새로 하나 받으면 됩니다. PowerShell 에서:

```powershell
& "$env:USERPROFILE\.vscode\extensions\anthropic.claude-code-*\resources\native-binary\claude.exe" setup-token
```

브라우저가 열리면 로그인하고 승인하세요. 터미널에 나오는 긴 글자를 복사해
`CLAUDE_CODE_OAUTH_TOKEN` 에 넣습니다.

> ⚠️ **발급 후 daily-brief 가 멀쩡한지 확인하세요.**
> 토큰을 새로 만들면 예전 토큰이 무효가 되는지 여부를 제가 확신할 수 없습니다.
> daily-brief 저장소의 **Actions → 아침 브리핑 → Run workflow** 를 눌러
> 한 번 돌려 보세요. 실패하면 방금 받은 새 토큰을 daily-brief 의
> `CLAUDE_CODE_OAUTH_TOKEN` 에도 똑같이 넣어 주면 됩니다.
> (두 저장소가 같은 토큰을 함께 쓰는 것은 문제없습니다)

> 💡 **이 토큰은 나중에 넣어도 됩니다.** 없으면 공지 알림·탭 버튼·캘린더는
> 전부 정상 동작하고, "공지 본문에서 일정을 자동으로 뽑는" 기능만 쉽니다.
> 일단 텔레그램 두 개만 넣고 시작해도 괜찮습니다.

---

## D. 코드 올리기 (3분)

이 폴더에서 PowerShell 을 열고, `<아이디>` 부분만 본인 GitHub 아이디로
바꿔서 한 줄씩 실행합니다.

```powershell
git init
git add -A
git commit -m "로스쿨 비서 첫 커밋"
git branch -M main
git remote add origin https://github.com/<아이디>/law-secretary.git
git push -u origin main
```

---

## E. 캘린더 웹주소 켜기 (2분)

1. 저장소 **Settings** → 왼쪽 메뉴에서 **Pages**
2. Source: **Deploy from a branch**
3. Branch: **`main`** / 폴더: **`/docs`** → **Save**
4. 1~2분 기다린 뒤 그 화면을 새로고침하면 주소가 나옵니다.
   `https://<아이디>.github.io/law-secretary/`

---

## F. 첫 실행 (5분)

1. 저장소의 **Actions** 탭 → 왼쪽에서 **공지 수집** 선택
2. 오른쪽 **Run workflow** → 초록색 **Run workflow** 버튼

2~3분 뒤 텔레그램에 이런 메시지가 옵니다:

```
📚 게시판 감시를 시작합니다
- 📝 LEET 일반사항 · 법학적성시험 공지사항 — 기존 10건은 알리지 않습니다
- 🎓 서강대학교 로스쿨 · 학사공지 — 기존 16건은 알리지 않습니다
...
```

⚠️ **기존 공지 57건에 대한 알림이 오지 않는 것이 정상입니다.**
지금 올라와 있는 글을 전부 "이미 본 것"으로 기록하고, **지금부터 새로
올라오는 글만** 알려 드립니다. 안 그러면 시작하자마자 알림 57개가 쏟아집니다.

---

## G. 아이폰 캘린더 연결 (2분)

1. 아이폰 **사파리**에서 E-4 의 주소를 엽니다.
2. **"📅 아이폰에 캘린더 추가하기"** 를 누릅니다 → **구독** → **완료**
3. ⚠️ **설정 → 캘린더 → 계정 → 로스쿨 일정** 으로 들어가서
   **"알림 제거"가 꺼져 있는지** 확인합니다.
   켜져 있으면 마감 알림이 전부 지워집니다.
4. 같은 화면에서 새로고침 주기를 **1시간마다**로 맞춥니다.

**minical 은 따로 설정할 것이 없습니다.** 아이폰 기본 캘린더를 그대로
읽어오기 때문에 자동으로 나타납니다.

**노트북**은 같은 주소를 구글 캘린더의 "URL로 캘린더 추가" 또는 아웃룩의
"인터넷 일정 구독" 에 넣으면 됩니다. 텔레그램 데스크톱 앱을 깔아 두면
알림도 노트북으로 함께 옵니다.

---

## 이제 어떻게 동작하나요

| 시각 | 하는 일 |
|---|---|
| 07:00~23:00, 30분마다 | 게시판 5곳 확인 → 새 글 있으면 텔레그램 알림 → 일정 추출 → 캘린더 갱신 |
| 매일 08:00 | 다가온 마감 알림 + 탭 버튼 메뉴 발송 |
| 10분마다 | 눌러 둔 버튼에 응답 |

텔레그램에서 언제든 쓸 수 있는 명령:

- `/menu` — 탭 버튼 보기
- `/upcoming` — 다가오는 마감 일정
- `/help` — 사용법

---

## 내 PC에서도 돌리고 싶다면 (선택)

클라우드가 기본이라 꼭 필요하지는 않습니다. 다만 학교 서버가 GitHub 의
IP를 막는 일이 생기면 이쪽으로 옮겨야 할 수 있습니다.

```powershell
python setup_telegram.py <봇토큰>   # 한 번만
python scrape.py --dry-run          # 보내지 않고 확인만
python scrape.py                    # 실제 실행
python bot_server.py                # 버튼에 바로바로 응답 (켜 두는 프로그램)
```

Claude Code 에서 `/notice` 를 실행하면 위 과정을 한 번에 해 줍니다.

---

## 문제가 생겼을 때

**알림이 하나도 안 옵니다**
→ A-6 을 하셨나요? 새 봇 대화방에서 `/start` 를 눌러야 봇이 말을 걸 수 있습니다.
→ Actions 탭에 빨간 X 가 있는지 확인해 주세요.

**알림이 갑자기 수십 개 왔습니다**
→ `state/seen.json` 이 지워진 것입니다. Actions 탭에서 무슨 일이 있었는지
   확인하고, 알려 주시면 되살려 드리겠습니다.

**캘린더에 일정이 안 보입니다**
→ 아직 새 공지가 없어서 뽑아낸 일정이 없을 수 있습니다.
   `https://<아이디>.github.io/law-secretary/law.ics` 를 브라우저에서 열어
   `BEGIN:VEVENT` 가 있는지 보면 알 수 있습니다.

**"본문이 첨부파일에 있어 자동 추출 불가" 라고 나옵니다**
→ 정상입니다. LEET 공지는 대부분 내용이 한글 파일(.hwp) 안에 들어 있는데,
   그 파일을 열려면 별도 프로그램이 필요합니다. 이럴 때는 알림에 붙은
   링크를 눌러 직접 확인해 주세요.

**Actions 가 어느 날부터 안 돕니다**
→ GitHub 은 저장소에 60일간 아무 변화가 없으면 예약 실행을 꺼버립니다.
   이 프로그램은 하루 한 번 `state/heartbeat.txt` 를 갱신해서 이를 막지만,
   그래도 멈췄다면 Actions 탭에서 **Enable workflow** 를 눌러 주세요.
